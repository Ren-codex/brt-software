/.phpunit.cache/
/_metadata.json
/node_modules
/public/hot
/public/storage
/storage/*.key
/vendor
.env
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode
nul
/.claude/worktrees/

# Vite writes this transiently while loading the config; committing it made
# the bundle hash flip between builds, which risks a manifest naming an asset
# that was never shipped.
vite.config.js.timestamp-*.mjs
